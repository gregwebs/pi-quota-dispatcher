{
  "name": "pi-quota-dispatcher",
  "version": "0.1.0",
  "private": false,
  "description": "Dynamically alter agent configuration based on quota",
  "license": "Apache-2.0",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi-extension",
    "pi",
    "quota"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gregwebs/pi-quota-dispatcher.git"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "files": [
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test test/dispatch.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-coding-agent": {
      "optional": true
    }
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.87.1",
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  }
}
